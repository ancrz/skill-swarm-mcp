{
  "mcpServers": {
    "skill-swarm": {
      "command": "python3.13",
      "args": ["-m", "skill_swarm.server"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src"
      }
    }
  }
}
